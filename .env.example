# Backend Server Configuration
PORT=3001

# Frontend API Configuration
# Leave empty to use relative URLs (recommended for production when frontend and backend are served together)
# Set to specific URL only if API is hosted separately from frontend
VITE_API_URL=

# Example for development if you want to override:
# VITE_API_URL=http://localhost:3001

# Example for production with separate API server:
# VITE_API_URL=https://api.example.com

# Home Assistant Configuration
# Note: These values are entered through the app's UI
# DO NOT commit your actual tokens to version control!
# HA_URL=http://homeassistant.local:8123
# HA_TOKEN=your_long_lived_access_token_here
# ENTITY_ID=sensor.power_consumption
