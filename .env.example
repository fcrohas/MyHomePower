# Server Configuration
PORT=3001

# Authentication - Change these in production!
APP_USERNAME=admin
APP_PASSWORD=admin

# Model Selection
# Choose which NILM model to use:
# - PowerTagPredictor: Original Conv2D-based seq2point model (default)
# - MSDCPredictor: Conv1D-based multi-scale NILM model
MODEL_TYPE=PowerTagPredictor

# MSDC Architecture (only used when MODEL_TYPE=MSDCPredictor)
# Options: S2P_on, S2P_State, S2P_State2
#
# S2P_on: Binary classification (ON/OFF only)
#   - Output: [Power] + [Probability 0-1]
#   - Example: [150W, 0.95] = "150W and 95% ON"
#   - Use for: Lamps, fans, simple heaters
#
# S2P_State: Multi-class classification (OFF/LOW/MED/HIGH/etc.)
#   - Output: [Power] + [Probability array]
#   - Example: [150W, [0.05, 0.10, 0.80, 0.05]] = "150W, 80% in state 2"
#   - Use for: Dishwashers, washing machines, multi-mode appliances
#
# S2P_State2: Same as S2P_State but with multi-scale feature extraction
#   - Parallel convolutions capture short & long patterns simultaneously
#   - Use for: Complex appliances with varying temporal patterns
#
MSDC_ARCHITECTURE=S2P_on

# MSDC Lite Mode (only used when MODEL_TYPE=MSDCPredictor)
# Set to 'true' to use lightweight version with 50% fewer parameters
# Use this if you get GPU out-of-memory errors
# LITE version: 16-16-20-25-25 filters, 512 dense units
# FULL version: 30-30-40-50-50 filters, 1024 dense units
#
# Memory usage comparison (approximate):
#   FULL + batch 1000: ~6-8 GB GPU RAM
#   FULL + batch 128:  ~2-3 GB GPU RAM
#   LITE + batch 1000: ~3-4 GB GPU RAM
#   LITE + batch 128:  ~1-2 GB GPU RAM
#
MSDC_LITE=false

# Example configurations:
# 1. Use original model:
#    MODEL_TYPE=PowerTagPredictor
#
# 2. Use MSDC with on/off detection:
#    MODEL_TYPE=MSDCPredictor
#    MSDC_ARCHITECTURE=S2P_on
#
# 3. Use MSDC with multi-state:
#    MODEL_TYPE=MSDCPredictor
#    MSDC_ARCHITECTURE=S2P_State
