# ML Feature Visual Architecture

## System Overview

```
┌──────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                              │
│                         (Vue 3 Frontend)                             │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────┐        ┌─────────────────────┐            │
│  │  📊 Power Tagging   │        │   🧠 ML Trainer      │            │
│  │      (Tab 1)        │        │      (Tab 2)         │            │
│  ├─────────────────────┤        ├─────────────────────┤            │
│  │ • Load power data   │        │ • Train Model       │            │
│  │ • Tag time ranges   │        │ • View progress     │            │
│  │ • View statistics   │        │ • Learning curves   │            │
│  │ • Export to JSON    │───────▶│ • Make predictions  │            │
│  └─────────────────────┘        └─────────────────────┘            │
│                                           │                          │
└───────────────────────────────────────────┼──────────────────────────┘
                                            │
                                    HTTP/SSE Requests
                                            │
                                            ▼
┌──────────────────────────────────────────────────────────────────────┐
│                      BACKEND API SERVER                              │
│                    (Express + TensorFlow.js)                         │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │                    API Endpoints                            │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │  POST /api/ml/train     ──▶  Train model with all data     │    │
│  │  GET  /api/ml/status    ──▶  Check training status         │    │
│  │  GET  /api/ml/history   ──▶  Get training metrics          │    │
│  │  POST /api/ml/predict   ──▶  Predict tag from power data   │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │                ML Processing Pipeline                       │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │                                                             │    │
│  │  1. Load Data         ┌──────────────────┐                │    │
│  │     ┌──────────────┐  │  dataDir/        │                │    │
│  │     │ loadAllData()│─▶│  power-data.json │                │    │
│  │     └──────────────┘  │  power-tags.json │                │    │
│  │                        └──────────────────┘                │    │
│  │              │                                              │    │
│  │              ▼                                              │    │
│  │  2. Preprocess Data                                        │    │
│  │     ┌─────────────────────┐                                │    │
│  │     │ prepareTrainingData │                                │    │
│  │     ├─────────────────────┤                                │    │
│  │     │ • Create windows    │                                │    │
│  │     │ • Resample to 60pts │                                │    │
│  │     │ • Normalize [0,1]   │                                │    │
│  │     │ • One-hot encode    │                                │    │
│  │     └─────────────────────┘                                │    │
│  │              │                                              │    │
│  │              ▼                                              │    │
│  │  3. Create Tensors                                         │    │
│  │     ┌───────────────┐                                      │    │
│  │     │ createTensors │                                      │    │
│  │     ├───────────────┤                                      │    │
│  │     │ • Train/Val   │                                      │    │
│  │     │ • Split 80/20 │                                      │    │
│  │     │ • Shuffle     │                                      │    │
│  │     └───────────────┘                                      │    │
│  │              │                                              │    │
│  │              ▼                                              │    │
│  │  4. Train Model                                            │    │
│  │     ┌─────────────────────────┐                            │    │
│  │     │  PowerTagPredictor      │                            │    │
│  │     ├─────────────────────────┤                            │    │
│  │     │  CNN1D × 5 windows      │                            │    │
│  │     │         ↓               │                            │    │
│  │     │  LSTM (128→64)          │                            │    │
│  │     │         ↓               │                            │    │
│  │     │  Dense + Softmax        │                            │    │
│  │     └─────────────────────────┘                            │    │
│  │              │                                              │    │
│  │              ▼                                              │    │
│  │  5. Save Model                                             │    │
│  │     ┌──────────────────┐                                   │    │
│  │     │ model.save()     │                                   │    │
│  │     └────────┬─────────┘                                   │    │
│  │              │                                              │    │
│  └──────────────┼──────────────────────────────────────────────┘    │
│                 │                                                   │
└─────────────────┼───────────────────────────────────────────────────┘
                  │
                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         DATA STORAGE                                 │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────┐    ┌────────────────────────────┐  │
│  │  Training Data (data/)     │    │  Trained Model             │  │
│  ├────────────────────────────┤    │  (server/ml/saved_model/)  │  │
│  │ • power-data-*.json        │    ├────────────────────────────┤  │
│  │ • power-tags-*.json        │    │ • model.json               │  │
│  │                            │    │ • weights.bin              │  │
│  │ Exported from UI           │    │ • metadata.json            │  │
│  │ after tagging              │    │                            │  │
│  └────────────────────────────┘    │ Auto-saved after training  │  │
│                                    └────────────────────────────┘  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

## Data Flow: Training

```
1. User Tags Data
   └─▶ Power consumption + time ranges + labels

2. User Exports Days
   └─▶ Saved to data/power-data-*.json & data/power-tags-*.json

3. User Clicks "Start Training"
   └─▶ POST /api/ml/train

4. Backend Loads Data
   └─▶ Reads all JSON files from data/

5. Data Preprocessing
   Raw Data Points
        ↓
   Create 10-min Windows
        ↓
   Resample to 60 timesteps
        ↓
   Normalize power values
        ↓
   Match tags to windows
        ↓
   One-hot encode tags
        ↓
   Create train/val tensors

6. Model Training
   For each epoch (50 total):
   ├─▶ Forward pass (predict)
   ├─▶ Calculate loss
   ├─▶ Backward pass (gradients)
   ├─▶ Update weights
   └─▶ Stream progress to frontend via SSE

7. Frontend Updates
   ├─▶ Progress bar advances
   ├─▶ Metrics display updates
   └─▶ Learning curves chart updates

8. Training Completes
   ├─▶ Save model to disk
   ├─▶ Save metadata
   └─▶ Send completion event
```

## Data Flow: Prediction

```
1. User Loads Power Data
   └─▶ Last 50 minutes of consumption

2. User Clicks "Predict Tag"
   └─▶ POST /api/ml/predict with power data

3. Backend Prepares Input
   Power Data Array
        ↓
   Extract last 50 minutes
        ↓
   Create 5 windows (10 min each)
        ↓
   Resample each to 60 timesteps
        ↓
   Normalize with saved stats
        ↓
   Create tensor [1, 5, 60, 1]

4. Model Inference
   Input Tensor
        ↓
   5× CNN1D branches
        ↓
   Concatenate features
        ↓
   LSTM processing
        ↓
   Dense layers
        ↓
   Softmax probabilities
        ↓
   Output: [p1, p2, p3, ..., pN]

5. Post-Processing
   ├─▶ Find argmax (predicted tag)
   ├─▶ Get confidence (max probability)
   └─▶ Sort all probabilities

6. Response
   {
     predictedTag: "climate",
     confidence: 0.87,
     allProbabilities: [...]
   }

7. Frontend Display
   ├─▶ Show predicted tag
   ├─▶ Display confidence %
   └─▶ Visualize top 5 probabilities
```

## Model Architecture Detailed

```
Input Shape: [batch, 5, 60, 1]
             (samples, windows, timesteps, channels)

┌─────────────────────────────────────────────────────────┐
│                    5 CNN1D Branches                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Window 1 ──┐    Window 2 ──┐    ...    Window 5 ──┐  │
│             │                │                       │  │
│    Conv1D(64, k=3)    Conv1D(64)          Conv1D(64) │  │
│       ↓               ↓                      ↓        │  │
│    Conv1D(64, k=3)    Conv1D(64)          Conv1D(64) │  │
│       ↓               ↓                      ↓        │  │
│    MaxPool1D(2)       MaxPool1D(2)         MaxPool1D │  │
│       ↓               ↓                      ↓        │  │
│    Conv1D(128)        Conv1D(128)          Conv1D(128)│  │
│       ↓               ↓                      ↓        │  │
│    GlobalAvgPool      GlobalAvgPool        GlobalAvgPool│
│       ↓               ↓                      ↓        │  │
│     [128]            [128]      ...        [128]     │  │
│       └───────────────┴────────────────────┘         │  │
└─────────────────────────────────────────────────────────┘
                        ↓
              Concatenate: [640]
                        ↓
              Reshape: [5, 128]
                        ↓
┌─────────────────────────────────────────────────────────┐
│                   LSTM Layers                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│         LSTM(128, return_sequences=True)                │
│                  dropout=0.2                            │
│                     ↓                                   │
│         LSTM(64)                                        │
│                  dropout=0.2                            │
│                     ↓                                   │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│                  Dense Layers                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│         Dense(64, activation='relu')                    │
│                     ↓                                   │
│         Dropout(0.3)                                    │
│                     ↓                                   │
│         Dense(N_classes, activation='softmax')          │
│                     ↓                                   │
│         Output: [p_climate, p_heater, p_coffee, ...]    │
│                                                         │
└─────────────────────────────────────────────────────────┘

Total Parameters: ~200K (depends on number of classes)
```

## Timeline Visualization

```
Training Timeline (Typical 3 days of data):

Epoch  1: ████░░░░░░░░░░░░░░░░ Loss: 2.3456, Acc: 0.2341
Epoch  5: ████████░░░░░░░░░░░░ Loss: 1.2345, Acc: 0.5234
Epoch 10: ████████████░░░░░░░░ Loss: 0.8234, Acc: 0.6789
Epoch 20: ████████████████░░░░ Loss: 0.4532, Acc: 0.8234
Epoch 30: ██████████████████░░ Loss: 0.3456, Acc: 0.8756
Epoch 40: ███████████████████░ Loss: 0.2891, Acc: 0.9023
Epoch 50: ████████████████████ Loss: 0.2567, Acc: 0.9156

Total Time: 3m 24s

Learning Curve Pattern (Good Training):

Loss ↓                  Accuracy ↑
2.5 |╲                 1.0 |        ╱─────
2.0 | ╲                0.8 |      ╱
1.5 |  ╲               0.6 |    ╱
1.0 |   ╲              0.4 |  ╱
0.5 |    ╲____         0.2 |╱
0.0 |_________╲        0.0 |_______________
    0   25   50            0   25   50
       Epochs                   Epochs

─── Training    ─── Validation
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────┐
│                  PowerViewer.vue                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  activeTab: 'tagging' | 'ml'                           │
│  powerData: Array<{timestamp, power}>                   │
│                                                         │
│  ┌──────────────────┐        ┌──────────────────┐     │
│  │ Tagging Tab      │        │ ML Tab           │     │
│  ├──────────────────┤        ├──────────────────┤     │
│  │ PowerChart.vue   │        │ MLTrainer.vue    │     │
│  │ TagManager.vue   │        │ :powerData       │     │
│  └──────────────────┘        └────────┬─────────┘     │
│                                        │               │
└────────────────────────────────────────┼───────────────┘
                                         │
                        Props: powerData │
                                         │
                                         ▼
┌─────────────────────────────────────────────────────────┐
│                   MLTrainer.vue                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Data:                                                  │
│  • trainingInProgress: boolean                          │
│  • trainingHistory: Array<metrics>                      │
│  • currentEpoch: number                                 │
│  • modelTrained: boolean                                │
│  • predictionResult: object                             │
│                                                         │
│  Methods:                                               │
│  • startTraining() ──▶ SSE stream from backend         │
│  • testPrediction() ──▶ POST to /api/ml/predict        │
│  • updateChart() ──▶ Update Chart.js visualization     │
│                                                         │
│  Components:                                            │
│  • Training controls (buttons)                          │
│  • Progress bar                                         │
│  • Metrics cards                                        │
│  • Learning curves chart (Chart.js)                     │
│  • Prediction interface                                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

This visual architecture shows the complete system from UI to storage!
